#include <stdio.h>
int main()
{
    int a, b, c;
    

    printf("Enter 3 numbers:\n");
    scanf("%d%d%d",&a, &b, &c);

    if (a>b && a>c)
    {
printf("The greatest number is %d\n",a);
    }
    if (b>a && b>c)
    {
        printf("The greatest number is:%d\n",b);
    }
    if (a==b==)
    {
        printf("All\n");
    }
    
    else
    {
        printf("The greates number is: %d\n",c);
    }
    
    
    
    return 0;
}